- link:
    ~/.zshrc: zshrc_mac
    ~/.tmux.conf: tmux.conf.mac
    ~/.config/aerospace: config/aerospace
    ~/.config/alacritty: config/alacritty

- shell:
    - [echo "Setting up desktop-specific configurations", Setting up desktop]
